/* Shared configuration is currently defined near the top of index.html.
   Main items: COLS, ROWS, graph, obstacles, bots, taskQueue, BOT_DEFS, TASK.
   This file documents the modules; index.html remains the single-file runnable demo. */
